import { StyledSVG } from "./../../SharedStyles";
import styled from "styled-components";

export const DiscordStyling = styled(StyledSVG)`
	color: #5865f2; //"blurple" from discord's brand guidelines
	height: auto;
`;
